# dependencies
node_modules/
.pnpm-store/

# build output
dist/
*.tsbuildinfo

# turbo / caches
.turbo/
coverage/

# env & secrets — never commit
.env
.env.*
!.env.example

# local deploy config (real Azure resource names) — keep out of the public repo
scripts/deploy.local.ps1

# local data / runtime
*.log
.DS_Store
Thumbs.db

# editor
.idea/
.vscode/*
!.vscode/extensions.json

# drizzle local artifacts
packages/data/drizzle/meta/_journal.json.bak
