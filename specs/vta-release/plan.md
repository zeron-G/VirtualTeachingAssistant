# Plan

1. Copy only deployer source and tests into a clean release directory.
2. Embed and genericize Course TA skill code and prompt references.
3. Add Linux bootstrap, documentation, ignore rules, CI, and security scanning.
4. Run tests, compile, build, inspect package contents, and scan for sensitive
   data.
5. Initialize Git, commit the audited release, create public GitHub repository
   `VirtualTeachingAssistant`, and push the default branch.
