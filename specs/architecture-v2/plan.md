# Architecture V2 Plan

1. Establish architecture, threat model, ADRs, and production gates.
2. Add immutable domain types and typed ports.
3. Implement policy, circuit breaker, and ordered agent fallback.
4. Implement restricted Codex/OpenClaw process adapters and native adapter port.
5. Implement LLM credential failover with production OAuth guardrails.
6. Add channel, skill, and activity registries.
7. Add minimized audit events, health supervisor, and runtime config validation.
8. Add fault-injection tests, architecture checks, packaging, and CI gates.
9. Rename project/repository to `VirtualTeachingAssistant` and publish.
